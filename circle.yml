machine:
  node:
    version: 4.3.0
  services:
    - docker

dependencies:
  pre:
    - docker build -t constellates/$CIRCLE_PROJECT_REPONAME:$CIRCLE_SHA1 .

test:
  override:
    # Lint
    - npm run lint
    # Run unit tests
    - npm run test