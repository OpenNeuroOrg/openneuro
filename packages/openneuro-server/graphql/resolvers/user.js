/**
 * User resolvers
 *
 * These are passthroughs to SciTran until we have authentication working internally
 */

export const users = () => {
  return []
}

export const whoami = () => {
  return {}
}
