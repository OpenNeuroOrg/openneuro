/**
 * Toolbar actions for the datasets / snapshots pages.
 *
 * Handled by WarnButton - hence callbacks
 *
 * Most of these are just redirects
 */
export const download = url => callback => {
  // Download trigger here
}

export const publish = datasetId => callback => {
  // Publish
}

export const del = datasetId => callback => {}

export const share = datasetId => callback => {}

export const snapshot = datasetId => callback => {}

export const follow = datasetId => callback => {}

export const star = datasetId => callback => {}
