comment: false

coverage:
  status:
    project:
      default: off
      app:
        flags: app
      server:
        flags: server

flags:
  app:
    paths:
      - app
  server:
    paths:
      - server
